import { EqualButton } from './EqualButton'

export default {
  title: 'Components/Buttons/EqualButton',
  component: EqualButton,
  argTypes: {
    onClick: { action: 'equals' },
  },
}

export const Equal = {}
