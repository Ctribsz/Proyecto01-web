import { DecimalButton } from './DecimalButton'

export default {
  title: 'Components/Buttons/DecimalButton',
  component: DecimalButton,
  argTypes: {
    onClick: { action: 'decimal' },
  },
}

export const Decimal = {}
